environments {
  default {
        webdriver.base.url = "https://www.google.com/travel/flights"
  }
}

webdriver {
  driver = chrome
  use.driver.service.pool = false
  timeouts {
      implicitlywait = 5000
      fluentwait = 5000
    }
}